CREATE TABLE [dbo].[TestTableWithIndexes4LockII] (
    [id]     INT IDENTITY (1, 1) NOT NULL,
    [FieldI] INT NULL,
    CONSTRAINT [pkTestTableWithIndexes4LockII] PRIMARY KEY CLUSTERED ([id] ASC)
);

