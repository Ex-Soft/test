let phrases;

exports.connect = function() {
    phrases = require("./uk");
}

exports.getPhrase = function(key) {
    if (!phrases[key]) {
        throw new Error(`${key} doesn't exist`);
    }

    return phrases[key];
}