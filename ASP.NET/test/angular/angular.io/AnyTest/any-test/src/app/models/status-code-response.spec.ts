import { StatusCodeResponse } from './status-code-response';

describe('StatusCodeResponse', () => {
  it('should create an instance', () => {
    expect(new StatusCodeResponse()).toBeTruthy();
  });
});
