import { DerivedGenericClass } from './derived-generic-class';

describe('DerivedGenericClass', () => {
  it('should create an instance', () => {
    expect(new DerivedGenericClass('', '')).toBeTruthy();
  });
});
